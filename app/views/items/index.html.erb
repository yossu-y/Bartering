<h2>投稿一覧</h2>

<% @items.each do |item| %>
　<%= item.title %>
　<%= item.body %>
　<% @item.item_images.each do |item_image| %>
　　<%= attachment_image_tag item_image, :image, size: "130x130" %>
　<% end %>
<% end %>